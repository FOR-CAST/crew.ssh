% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crew_ssh_check.R
\name{crew_ssh_check}
\alias{crew_ssh_check}
\title{Preflight-check SSH worker nodes}
\usage{
crew_ssh_check(
  nodes,
  projdir,
  rscript = NULL,
  homogeneous = TRUE,
  ssh_options = c("-o", "BatchMode=yes", "-o", "ConnectTimeout=10"),
  packages = "crew",
  timeout = 30
)
}
\arguments{
\item{nodes}{Either a named integer vector mapping host names to per-node
worker capacities (for example \code{c(node1 = 8L, node2 = 16L)}), or a list of
\code{\link[=crew_ssh_node]{crew_ssh_node()}} objects when nodes need different \code{rscript} / \code{projdir} /
\code{ssh_options}. The controller's total \code{workers} is the sum of the per-node
capacities, and workers are placed in proportion to them.}

\item{projdir}{Path to the project directory on the remote nodes (identical
across nodes unless overridden per node via \code{\link[=crew_ssh_node]{crew_ssh_node()}}). Each worker
runs \verb{cd <projdir>} first so the project \code{.Rprofile} / \code{renv} activate and
relative paths resolve.}

\item{rscript}{Name or path of the \code{Rscript} executable on the remote nodes.
\code{NULL} (the default) is resolved per \code{homogeneous}, mirroring the
\code{\link[parallelly:makeClusterPSOCK]{parallelly::makeClusterPSOCK()}} convention: the launching session's own
\code{Rscript} (\code{file.path(R.home("bin"), "Rscript")}, a full path that does not
depend on the remote \code{PATH} or on rig-style version-suffixed names) when
\code{homogeneous = TRUE}, or bare \code{"Rscript"} found on the remote \code{PATH} when
\code{homogeneous = FALSE}. Override per node via \code{\link[=crew_ssh_node]{crew_ssh_node()}}.}

\item{homogeneous}{Logical: do all nodes share the same R installation path as
the control machine? This is the package's default assumption and controls
how a \code{NULL} \code{rscript} is resolved. Set \code{FALSE} for mixed installs (then the
remote \code{PATH} must provide \code{Rscript}, or set \code{rscript} per node).}

\item{ssh_options}{Character vector of options passed to the \code{ssh} client
before the host (for example \code{c("-o", "BatchMode=yes")}).}

\item{packages}{Character vector of package names whose availability to check
on each node (default \code{"crew"}, which every worker needs).}

\item{timeout}{Per-SSH-command timeout in seconds.}
}
\value{
A data frame (class \code{crew_ssh_check}), one row per node, with columns
\code{host}, \code{ssh}, \code{projdir}, \code{rscript}, \code{r_version}, \code{missing_packages}, \code{ok},
and \code{detail} (diagnostic text for the first failing step).
}
\description{
\strong{Experimental.} For each node, verify over SSH (in order) that the host
is reachable, the project directory exists, the resolved \code{Rscript} runs (and
report its R version), and the required \code{packages} load. Use this before a run
to catch setup problems early, instead of debugging a pipeline that silently
launches no workers.
}
\examples{
\dontrun{
crew_ssh_check(
  nodes = c(host1 = 1L, host2 = 1L),
  projdir = "/home/me/myproject",
  packages = c("crew", "targets")
)
}
}
\seealso{
Other ssh:
\code{\link{crew_class_launcher_ssh}},
\code{\link[=crew_controller_ssh]{crew_controller_ssh()}},
\code{\link[=crew_ssh_monitor]{crew_ssh_monitor()}},
\code{\link[=crew_ssh_node]{crew_ssh_node()}}
}
\concept{ssh}
