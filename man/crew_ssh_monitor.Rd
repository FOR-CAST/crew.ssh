% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crew_ssh_monitor.R
\name{crew_ssh_monitor}
\alias{crew_ssh_monitor}
\title{Live CPU and memory monitor for SSH worker nodes}
\usage{
crew_ssh_monitor(
  nodes,
  interval = 2,
  rscript = NULL,
  homogeneous = TRUE,
  ssh_options = NULL,
  timeout = 10,
  viewer = NULL
)
}
\arguments{
\item{nodes}{Either a named integer vector mapping host names to per-node
worker capacities (for example \code{c(node1 = 8L, node2 = 16L)}), or a list of
\code{\link[=crew_ssh_node]{crew_ssh_node()}} objects when nodes need different \code{rscript} / \code{projdir} /
\code{ssh_options}. The controller's total \code{workers} is the sum of the per-node
capacities, and workers are placed in proportion to them.}

\item{interval}{Refresh interval in seconds (default \code{2}).}

\item{rscript}{Name or path of the \code{Rscript} executable on the remote nodes.
\code{NULL} (the default) is resolved per \code{homogeneous}, mirroring the
\code{\link[parallelly:makeClusterPSOCK]{parallelly::makeClusterPSOCK()}} convention: the launching session's own
\code{Rscript} (\code{file.path(R.home("bin"), "Rscript")}, a full path that does not
depend on the remote \code{PATH} or on rig-style version-suffixed names) when
\code{homogeneous = TRUE}, or bare \code{"Rscript"} found on the remote \code{PATH} when
\code{homogeneous = FALSE}. Override per node via \code{\link[=crew_ssh_node]{crew_ssh_node()}}.}

\item{homogeneous}{Logical: do all nodes share the same R installation path as
the control machine? This is the package's default assumption and controls
how a \code{NULL} \code{rscript} is resolved. Set \code{FALSE} for mixed installs (then the
remote \code{PATH} must provide \code{Rscript}, or set \code{rscript} per node).}

\item{ssh_options}{Character vector of options passed to the \code{ssh} client
before the host (for example \code{c("-o", "BatchMode=yes")}).}

\item{timeout}{Per-poll SSH timeout in seconds (default \code{10}).}

\item{viewer}{A \pkg{shiny} viewer function controlling where the gadget
opens. \code{NULL} (the default) uses \code{\link[shiny:paneViewer]{shiny::paneViewer()}} (the Viewer pane);
pass \code{\link[shiny:browserViewer]{shiny::browserViewer()}} to open in a web browser instead.}
}
\value{
Invisibly, the gadget's return value (\code{NULL}); called for its side
effect of opening the dashboard.
}
\description{
\strong{Experimental.} Open a small dashboard that polls each node over SSH on
a timer and shows CPU and memory use, one row per machine, in a single window.
It is the runtime companion to \code{\link[=crew_ssh_check]{crew_ssh_check()}}: where the preflight is a
one-shot diagnostic, this keeps refreshing so you can watch a running cluster
without opening a terminal and \code{htop} per node. The dashboard is a
\pkg{miniUI} gadget and renders in the RStudio / VS Code Viewer pane (the same
place \code{targets::tar_visnetwork()} appears), so you can alt-tab to it.
}
\details{
Worker stats are read directly from \verb{/proc} (\verb{/proc/stat}, \verb{/proc/meminfo},
\verb{/proc/loadavg}), so the \strong{nodes must run Linux}; nothing beyond a POSIX
shell is required on them. Nodes are polled in parallel, and connections are
multiplexed (\code{ControlMaster}) so each refresh reuses one SSH connection per
host.

Requires the \pkg{shiny}, \pkg{miniUI}, and \pkg{htmltools} packages (in
\code{Suggests}); install them to use this function.
}
\examples{
\dontrun{
# monitor the same nodes used by the controller / preflight
crew_ssh_monitor(nodes = c(host1 = 1L, host2 = 1L))

# in a project that sets the crew.ssh.nodes option
crew_ssh_monitor(getOption("crew.ssh.nodes"))
}
}
\seealso{
Other ssh:
\code{\link{crew_class_launcher_ssh}},
\code{\link[=crew_controller_ssh]{crew_controller_ssh()}},
\code{\link[=crew_ssh_check]{crew_ssh_check()}},
\code{\link[=crew_ssh_node]{crew_ssh_node()}}
}
\concept{ssh}
